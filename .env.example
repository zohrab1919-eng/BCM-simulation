# Copy this file to .env and fill in your values

# Port the server listens on (Railway sets this automatically)
PORT=3000

# Public URL — set this to your Railway/Render domain when deployed
# Leave blank for localhost (auto-detected)
BASE_URL=

# Optional: DeepSeek API key (for future AI debrief features)
DEEPSEEK_API_KEY=
